<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <a class="navbar-brand ml-md-5 {% if current_menu == 'homepage'%} text-info{% else %} text-primary{% endif %}" href="{{ path('homepage') }}"><h1>My Blog</h1></a>
        <ul class="navbar-nav mr-auto ml-5 mt-2 mt-lg-0">
            <li class="nav-item">
                <a class="nav-link {% if current_menu == 'blog' %}active{% endif %}" href="{{ path('blog') }}">Blog</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if current_menu == 'about' %}active{% endif %}" href="{{ path('about') }}">About</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if current_menu == 'contact' %}active{% endif %}" href="{{ path('contact') }}">Contact</a>
            </li>
        </ul>
    </div>
    <div>
        <a class="btn btn-outline-primary mr-4" href="{{ path('register') }}">S'inscrire</a>
        <a class="btn btn-primary" href="{{ path('app_login') }}">Se connecter</a>
    </div>
</nav>